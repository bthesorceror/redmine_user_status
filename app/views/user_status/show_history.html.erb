<% html_title l(:l_update_history_for) + @user.name %>

<%= render "header" %>

<% if Setting.gravatar_enabled? %>
  <p>
    <%= avatar @user, :size => 75%>
  </p>
<% end %>

<h3><%= l(:l_rss_update_for) + ' ' + @user.name %></h3>
<p>
  <%= link_to "Back", request.referer %>
</p>

<%= render "list" %>
