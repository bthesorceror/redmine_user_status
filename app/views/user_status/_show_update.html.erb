
<div class="status_div">
<table> <tr><td style="vertical-align: top;">
<%= avatar(user, :size => 50)%>
</td>
<td>
<b style="font-size: 11pt;"><%= link_to user.name, :action => 'show_history', :user_id => user.id %></b><br />
<% if user.last_update %>
				<p class="status_message"><%= textilizable(h user.last_update.status) %></p>
        <div class="status_time"><%= format_time(user.last_update.created_at) rescue l(:l_no_timestamp) %></div>
<% else %>
  <p><%= l(:l_no_update_found) %></p>
<% end %>
</td>
</tr>
</table>
</div>
